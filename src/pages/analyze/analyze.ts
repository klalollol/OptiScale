// analyze.ts — Analyze page (no interactive state needed; data is static)
import { analysisData, projectData } from '../../demo-data';

// Keep TypeScript strict — acknowledge imports are used for future backend wiring.
void analysisData;
void projectData;
